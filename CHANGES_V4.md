# تغییرات نسخه ۴ (رفع مشکل UI/دیتای واقعی)

## ۱. مشکل شما: "ظاهر درست ولی دیتای واقعی نیست"
داشبورد Debug داده‌ی واقعی نشان می‌داد چون مستقیماً از IqosConnectionManager.Listener
می‌خواند، اما بخش‌های Devices تب اصلی هیچ سوییچ/ویجتی برای این فرمان‌ها نداشتند -
فقط "Smart Gesture" واقعی وصل بود. FlexPuff هم به‌صورت hardcode "غیرفعال" نمایش
داده می‌شد در حالی‌که پروتکل SCP فرمان enable/disable آن را (تایید شده از
hauntedfail/iqos) از قبل داشت.

## ۲. کارهایی که انجام شد
- ۶ ویجت واقعی جدید به تب Devices اضافه شد که مستقیماً IqosConnectionManager.sendCommand
  را صدا می‌زنند و روی BLE/USB واقعی می‌نویسند: Autostart، Vibration، Illumination
  Mode، Pause Mode، FlexPuff (فعال شد)، و FlexBattery (به‌صورت نمایش زنده باتری چون
  در پروتکل SCP فرمان on/off برای آن کشف نشده - فقط Load/Read دارد).
- هر سوییچ اگر دستگاه متصل نباشد به جای دروغ گفتن، Toast می‌دهد "دستگاه متصل نیست"
  و مقدار سوییچ عملاً به دستگاه ارسال نمی‌شود.
- نوبار پایین از متن به آیکون+برچسب (شبیه اپ اصلی IQOS) تغییر کرد.
- دکمه Rename Device اضافه شد (در SharedPreferences ذخیره می‌شود).
- ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION برای تمام نسخه‌های اندروید
  (نه فقط API<=30) در Manifest درخواست می‌شود، دقیقاً مثل اپ اصلی IQOS - چون خیلی
  از ROMها (MIUI/ColorOS/One UI) حتی با فلگ neverForLocation باز هم اسکن BLE رو
  بدون مجوز لوکیشن فیلتر می‌کنند.

## ۴. چرا "هیچی عوض نشد" و نسخه هنوز 3.0.0 بود
این علت اصلی مشکل شما بود، نه چیزی که در v4 اضافه شده باشد:

`app/build.gradle` برای build.type های debug و release از `signingConfigs.debug`
پیش‌فرض گریدل استفاده می‌کرد. این کیستور پیش‌فرض **در هر اجرای GitHub Actions از صفر
ساخته می‌شود** (چون runner یک ماشین مجازی تازه است)، یعنی هر بار که push می‌کنید یک
کلید امضای رندوم و متفاوت تولید می‌شود. نتیجه: وقتی APK جدید را روی گوشی نصب می‌کنید،
اندروید امضای جدید را با امضای نسخه‌ی قبلی که از قبل نصب است مقایسه می‌کند، می‌بیند
مطابقت ندارد، و به‌صورت بی‌سروصدا نصب/آپدیت را رد می‌کند یا برنامه‌ی قدیمی دست‌نخورده
باقی می‌ماند - دقیقاً همان چیزی که دیدید: هیچ تغییری ظاهر نشد و نسخه هنوز 3.0.0 بود.

**راه‌حل در این نسخه:** یک کیستور ثابت در مسیر `keystore/iqos-debug.keystore` ساخته و
داخل ریپو کامیت شد؛ `build.gradle` حالا هم برای debug و هم release صراحتاً همین فایل را
استفاده می‌کند (`signingConfigs.fixedDebug`)، پس امضا در تمام بیلدهای آینده ثابت می‌ماند.
همچنین versionCode به 8 و versionName به "4.0.0" ارتقا یافت تا بعد از نصب موفق واقعاً
ببینید که فرق کرده.

### قبل از نصب این نسخه حتماً:
1. اگر نسخه‌ی قبلی (3.0.0) روی گوشی نصب است، **اول Uninstall کنید** - چون امضای آن با
   کلید رندوم قدیمی بود و با کلید ثابت جدید سازگار نیست؛ اندروید نصب مستقیم را رد می‌کند.
2. کل محتویات این زیپ را روی ریپوی GitHub خودتان push کنید (فایل باینری کیستور هم باید
   کامیت شود - در .gitignore مستثنی نشده).
3. Actions → Build Android APKs را اجرا کنید (یا با push خودکار اجرا می‌شود).
4. از artifact جدید `app-debug` دانلود و نصب کنید. حالا در Settings → About باید
   "نسخه 4.0.0 (بیلد 8)" را ببینید.

