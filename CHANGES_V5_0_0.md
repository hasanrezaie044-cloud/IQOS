# v5.0.0 - فرمان‌های تأییدشده + خواندن کامل وضعیت دستگاه

هیچ قابلیتی حذف نشد. تمام کلاس‌ها، تب‌ها، سوییچ‌ها، پشتیبان‌گیری، آمار مصرف، دیباگ و مسیر USB سر جای خودشان هستند؛ فقط اصلاح و اضافه شده‌اند.

## ۱. ریشه اصلی «فرمان‌ها الکی هستند» پیدا شد

در `BleIqosTransport` مشخصه فرمان (SCP) فقط داخل یک سرویس ثابت جستجو می‌شد:

```kotlin
val rabService = gatt.getService(RAB_SERVICE_UUID) ?: gatt.getService(RRP_SERVICE_UUID)
val scpChar = rabService.getCharacteristic(SCP_CONTROL_POINT_UUID)   // اگر سرویس اول پیدا می‌شد ولی این مشخصه را نداشت → null
```

اگر دستگاه سرویس اول را داشت اما مشخصه SCP در سرویس دیگری بود، نتیجه `null` می‌شد و:
- نوتیفیکیشن‌ها هرگز فعال نمی‌شدند (پس هیچ پاسخی از دستگاه نمی‌رسید)،
- و فرمان‌ها بی‌صدا دور ریخته می‌شدند در حالی که UI می‌گفت «ارسال شد».

حالا مشخصه در **تمام** سرویس‌های کشف‌شده جستجو می‌شود و سرویس واقعی‌اش در لاگ و صفحه پیشرفته نمایش داده می‌شود.

## ۲. هر فرمان حالا با خواندن از دستگاه تأیید می‌شود

قبلاً به محض تحویل بایت‌ها به بلوتوث، پیام موفقیت نشان داده می‌شد. الان هر فرمان یکی از این نتایج را می‌گیرد:

- «تأیید شد توسط دستگاه» (نوشتن + خواندن مجدد + مقدار عوض شده)
- «دستگاه فرمان را نپذیرفت»
- «دستگاه پاسخی نداد»
- «ارسال شد (این قابلیت فرمان خواندن وضعیت ندارد)» — مثل قفل و لرزش فیزیکی
- «دستگاه متصل نیست»

اگر دستگاه قبول نکند، سوییچ به مقدار واقعی دستگاه برمی‌گردد؛ دیگر UI دروغ نمی‌گوید.

## ۳. باگ چک‌سام فلکس‌پاف

در مرجع متن‌باز، فریم فعال‌کردن و غیرفعال‌کردن فلکس‌پاف چک‌سام یکسان `0x0A` دارند. با تحلیل خطی چک‌سام روی تمام فریم‌های منتشرشده، مقدار درست فریم غیرفعال‌سازی `0x61` است (دو مسیر مستقل به همین عدد می‌رسند). برنامه اول فریم رسمی را می‌فرستد؛ فقط اگر دستگاه واقعاً نپذیرفت، یک بار با چک‌سام اصلاح‌شده تلاش می‌کند و نتیجه را گزارش می‌دهد.

## ۴. فریم‌های پاسخ که قبلاً نادیده گرفته می‌شدند

- فریم تاریخ/روزهای سرویس (`80 02`) اصلاً پارس نمی‌شد.
- رمزگشای قبلی برای فریم‌های ناشناس حدس می‌زد: «اگر بایت ۲ بین ۰ و ۱۰۰ است، پس درصد باتری است» — همین باعث می‌شد پاسخ تنظیمات به‌جای وضعیت، درصد باتری خوانده شود.

رمزگشای جدید بر اساس هدرهای مستند کار می‌کند و هر فریم را دقیقاً شناسایی یا صریحاً «ناشناخته» گزارش می‌کند.

## ۵. اطلاعات جدیدی که از دستگاه خوانده می‌شود

فریم‌ور بدنه و هولدر، شماره محصول بدنه و هولدر، سازنده، شماره مدل، سریال، نسخه سخت‌افزار/نرم‌افزار، ولتاژ سل باتری (میلی‌ولت)، درصد شارژ اعلامی دستگاه، درصد تخمینی از منحنی ولتاژ، مجموع پاف عمر دستگاه، روزهای در سرویس، قدرت سیگنال، MTU، وضعیت جفت‌سازی، مدت اتصال، و شمارش فریم‌های سالم/ناشناخته.

## ۶. قابلیت‌های جدید کنترلی

- حالت فلکس‌باتری (اکو / پرفورمنس) — قبلاً «فرمانش کشف نشده» بود.
- تنظیمات واقعی لرزش: شروع گرم شدن، آماده شدن برای مصرف، نزدیک پایان پاف، خاتمه دستی، و شروع شارژ (مدل‌های هولدردار) — قبلاً یک کلید تکی بود.
- قفل و بازکردن قفل دستگاه (دنباله سه‌فریمی واقعی).
- خواندن روشنایی، ژست هوشمند، روشن‌شدن خودکار و حالت توقف از خود دستگاه.
- تب جدید «پیشرفته»: گزارش فنی کامل، لیست قابلیت‌های مدل، تاریخچه نتیجه فرمان‌ها، کاوش کامل سرویس‌ها و مشخصه‌های GATT، و کپی گزارش.

## ۷. پایداری اتصال

اتصال مستقیم به دستگاه قبلی (بدون اسکن)، تلاش برای دستگاه جفت‌شده وقتی اسکن چیزی پیدا نمی‌کند، اتصال مجدد خودکار با تأخیر پله‌ای، درخواست MTU بزرگ‌تر (دنباله لرزش شارژ ۲۰ بایتی است و در MTU پیش‌فرض جا نمی‌شود)، و استفاده از APIهای جدید نوشتن/خواندن اندروید ۱۳+ به‌جای APIهای منسوخ.

## ۸. تست و بیلد

- ۲۰ تست واحد JVM اضافه شد (`app/src/test/...`) که رمزگشا و سازنده فرمان‌ها را با بردارهای تست خودِ مرجع می‌سنجد. اجرا: `gradle testDebugUnitTest`.
- فایل `.github/workflows/build.yml` اضافه شد (در زیپ قبلی وجود نداشت، هرچند README به آن اشاره کرده بود). تست‌ها + APK دیباگ و ریلیز را می‌سازد و آپلود می‌کند.
- `versionCode 11` / `versionName 5.0.0`.

## چیزهایی که واقعاً قابل پیاده‌سازی نیستند

- **دمای تیغه گرم‌کن:** دستگاه هیچ مشخصه یا فرمان مستندی برای دما نمی‌دهد. هر عدد دمایی که نمایش داده شود ساختگی است، پس نمایش داده نمی‌شود. دکمه «کاوش مشخصه‌ها» تمام مقادیر خواندنی دستگاه را نشان می‌دهد تا خودتان بتوانید ببینید چه چیزی واقعاً وجود دارد.
- **تغییر دما / پروفایل گرمایش / محدودیت‌های ایمنی:** در فریم‌ور قفل است.
- **آپدیت فریم‌ور:** فقط با اپ رسمی و سرور سازنده.
- **خواندن وضعیت قفل:** فرمان قفل واقعی است، اما فرمان خواندن وضعیت قفل مستند نشده، پس تأیید خودکار ندارد.
- **خواندن وضعیت ژست هوشمند:** فریم خواندنش منتشر نشده بود؛ از رابطه خطی چک‌سام در همان خانواده فریم‌ها استخراج شد (سه جفت فریم منتشرشده تأییدش می‌کنند) و اگر دستگاه جواب نداد، «نامشخص» می‌شود نه خطا.

## نکته مهم درباره بیلد

فایل `gradle/wrapper/gradle-wrapper.jar` در پروژه وجود ندارد، پس `./gradlew` بدون آن کار نمی‌کند. workflow جدید مستقیماً از Gradle 8.5 استفاده می‌کند و به wrapper نیازی ندارد. اگر لوکال بیلد می‌گیرید، پروژه را در Android Studio باز کنید یا یک بار `gradle wrapper` بزنید.
