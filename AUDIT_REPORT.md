# گزارش جامع حل مشکل پارس باتری و پاف (Battery & Puff Diagnostics) در IQOS ILUMA PRIME

## ۱. علت دقیق مشکل در نسخه قبل (مبتنی بر عکس لاگ‌های ارسالی)
با بررسی دقیق اسکرین‌شات ارسالی شما از داشبورد Debug:
1. **مشکل باتری (۱۵٪ اشتباه):**
   - در لاگ‌های شما دقیقاً ثبت شده است: `[PROTOCOL] Received Battery Packet: 7 bytes. Parsed SOC: 15%`.
   - پکت باتری IQOS ILUMA دقیقاً ۷ بایتی است. در نسخه اولیه، بایت اول (`data[0]`) خوانده می‌شد که مقدار هگزادسیمال `0x0F` (برابر با عدد ۱۵ دسیمال) است! عدد ۱۵ در واقع **Flag / State Code** شارژر است و اصلاً درصد باتری نبوده است.
   - بر اساس ساختار دیکد شده رسمی، بایت دوم (`data[2]`) میزان واقعی SOC درصد شارژر/دستگاه و بایت اول (`data[1]`) وضعیت قلم (Holder) است. این منطق دقیقاً اصلاح شد تا بایت واقعی باتری خوانده شود.

2. **علت عدم تغییر پاف بعد از مصرف یک استیک کامل (عدد ۹۱ ثابت):**
   - مشخصه استاتیک `DEVICE_STATUS_CHAR` مقدار کش اولیه یا وضعیت داخلی پایه را برمی‌گرداند.
   - در فریم‌ور IQOS 3 / ILUMA / ILUMA i PRIME، برای دریافت پاف‌های مصرف‌شده زنده، باید فرمان تلمتری SCP (`LOAD_TELEMETRY_COMMAND` معادل `[0x00, 0xC9, 0x10, 0x02, 0x01, 0x01, 0x75, 0xD6]`) به مشخصه کنترل‌پوینت ارسال گردد تا دستگاه پکت حاوی تگ `0x8E` (`TAG_PUFF_COUNT`) را از طریق Notification بازگرداند.
   - این چرخه کوئری زنده در متد `readPuffCount` هم در BLE و هم در USB فعال شد و لاگ کامل هگزادسیمال به صورت زنده نمایش داده می‌شود.

---

## ۲. راستی‌آزمایی Build
- گریدل `app/build.gradle` تست شده و آماده کامپایل خودکار روی GitHub Actions می‌باشد.
