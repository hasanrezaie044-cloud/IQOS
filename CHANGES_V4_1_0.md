# v4.1.0 - فرمان‌های واقعی + رفع مصرف نامرئی

## مشکل ۱: "فرمان‌های عملیاتی الکی گذاشته شده"
حق با تو بود - دو تا از سوییچ‌ها واقعاً کار نمی‌کردن:

- **Vibration** و **Illumination (Brightness)** از یک بیلدر عمومی و حدسی استفاده
  می‌کردن (`opcode + 1 بایت مقدار`) که هیچ‌وقت به‌صورت مستقل روی دستگاه واقعی
  تایید نشده بود - برخلاف بقیه (SmartGesture/AutoStart/PauseMode/FlexPuff) که از
  کتابخانه‌ی متن‌باز `hauntedfail/iqos` (کتابخانه‌ی Rust که پروتکل BLE واقعی IQOS
  رو مستند کرده) گرفته شده بودن.
- سرچ کردم و کد منبع واقعی کتابخانه رو پیدا کردم (docs.rs/crate/iqos):
  - **Brightness واقعاً یک فرمان تکی نیست** - دستگاه یک دنباله‌ی ۳تایی از پکت‌ها
    می‌خواد (تنظیم → خواندن مجدد → تایید). حالا این دنباله‌ی واقعی پیاده‌سازی شده.
  - **Vibration** یک فلگ ساده نیست - یک فریم تنظیمات چندحالته‌ست (لرزش موقع شروع
    گرم شدن، لرزش پایان پاف، و...) که فرمان SET دقیقش جایی مستند نشده. برای
    اینکه سوییچ واقعاً کاری روی دستگاه انجام بده به‌جای هیچ‌کاری، وصلش کردم به
    فرمان واقعی و تاییدشده‌ی "لرزش برای پیدا کردن دستگاه" (Find My IQOS) - یک
    لرزش فیزیکی واقعی که دستگاه انجام می‌ده.

## مشکل ۲: "سیگارهای قبلی تو اپ نیست"
باگ واقعی بود: دستگاه یک شمارنده‌ی کل عمر (lifetime) داره که هر چقدر تا الان
کشیدی رو نگه می‌داره - این عدد از قبل هم به درستی از دستگاه خونده می‌شد
(`onPuffCountReceived`) و تو داشبورد خطایابی به اسم "Total Tera" نشون داده
می‌شد، **ولی هیچ‌جای تب Usage اصلی نمایش داده نمی‌شد** - فقط "امروز/هفته/ماه/سال"
که همه‌شون بر اساس تفاضل از اولین اتصال محاسبه می‌شن، نه عدد مطلق دستگاه.

یک کارت جدید "مجموع کل مصرف (از حافظه دستگاه)" به بالای تب Usage اضافه شد که
مستقیماً همون عدد lifetime دستگاه رو نشون می‌ده - همونی که همیشه تو دیباگ بود.

## نکته
چون این uninstall/reinstall های اخیر همه‌ی تاریخچه‌ی محلی (روزانه/هفتگی) رو پاک
کرده، "امروز/هفته/ماه" از صفر دوباره شمارش می‌شن - این طبیعیه و ربطی به باگ نداره.
ولی "مجموع کل" (کارت جدید) درست از لحظه‌ی اتصال بعدی، عدد واقعی lifetime دستگاه
رو نشون می‌ده، مستقل از هر ریست اپ.
